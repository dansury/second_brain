# TODO — second_brain

Только **незакрытые** задачи. Закрытая задача **переносится** в `DONE.md`
(дата + строка «что сделано»), а не удаляется и не остаётся как `[x]`.
Порядок фаз и зависимости — `DEV_PLAN.md`.

Маркеры: `[ ]` не начато · `[~]` в работе · `[BLOCKED: причина]` пропустить
и идти дальше (маркер не снимать, пока причина не снята по-настоящему).

## M3 — Документы (слой 5)

- [ ] 3.1 Приём файла из Telegram (pdf/docx/txt) → текст. Определить, где
      парсер: в hermes (вложения) или в MCP-инструменте.
- [ ] 3.2 `obsidian_write_entry(type: document)` — прогон слоя 5 и запись в
      `Documents/` с frontmatter по `ТЗ.md` §6.
- [ ] 3.3 Уточняющие вопросы по документу (`Promts/05_document_intake.md`) —
      связать с `search_vault`, чтобы не переспрашивать уже известное.

## M4 — Vision: фото-люди (слой 6)

- [ ] 4.1 Приём фото → мультимодальная модель. Проверить, какие бесплатные
      модели OpenRouter принимают `image` (`list_free_models(modalities:["image"])`).
- [ ] 4.2 Дизамбигуация персон на фото через `lookup_character` + запись в
      `Photos/`.

## M5 — Рукописный текст (слой 7)

- [ ] 5.1 Расшифровка рукописи с подсказкой из `get_handwriting_profile`.
- [ ] 5.2 Правка расшифровки пользователем → `remember_handwriting` как
      часть 👎-потока слоя 9.

## Инфраструктура

- [ ] I.1 Обновление курса USD→RUB в `config/fx.json` — сейчас правится
      руками. Нужен либо крон в контейнере, либо публичный источник курса.
- [ ] I.2 Проверить фактические суточные лимиты бесплатных моделей в бою:
      `dailyQuota` из shir-man — оценка, а не контракт OpenRouter.
- [ ] I.3 `graphify update .` после правок кода — не автоматизировано,
      граф в образе (`graphify-out/graph.json`) устаревает между сборками.

## Открытые вопросы (`ТЗ.md` §13)

- [ ] Q.5 [BLOCKED: inline-кнопки 👍👎 в hermes-agent не подтверждены
      документацией] Слой 9 пока просит текстовую реакцию.
- [ ] Q.7 [BLOCKED: не подтверждено, обрабатывает ли hermes `channel_post`]
      Приёмник канала-дневника (§4, §10.2).
- [ ] Q.1 Бэкап vault-а: том amvera vs отдельный git-репозиторий/Obsidian Sync.
- [ ] Q.3 Хранить ли оригиналы фото/голоса в vault или только текстовые
      résumé — влияет на объём тома.
