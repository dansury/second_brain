# Feedback log (шаблон)

> Реальный журнал ведётся на persistent-томе (`OBSIDIAN_VAULT_PATH`), этот
> файл в git — только шаблон формата, см. `../06_feedback_followup.md`.
> Append-only: `secondbrain` только дописывает строки, никогда не редактирует
> и не удаляет существующие.

| timestamp (UTC) | layer | model | reaction | comment | ref |
|---|---|---|---|---|---|
