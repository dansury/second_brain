# DEV_PLAN — порядок работ и зависимости

Спутник `TODO.md`: там «что», здесь «в каком порядке и почему нельзя раньше».
Дорожная карта-первоисточник — `ТЗ.md` §12.

## Фазы

| Фаза | Задачи `TODO.md` | Зависит от | Статус |
|---|---|---|---|
| M1 — базовый конвейер и vault | — | текущий стек | закрыта (`DONE.md`) |
| M2 — консультант (слой 4) | — | M1 | закрыта (`DONE.md`) |
| M3 — документы (слой 5) | 3.1 → 3.2 → 3.3 | M1 | текущая |
| M4 — vision: фото-люди (слой 6) | 4.1 → 4.2 | M1 (People) + мультимодальная модель | ждёт M3 |
| M5 — рукописный текст (слой 7) | 5.1 → 5.2 | M4 (тот же vision-контур) | ждёт M4 |
| I — инфраструктура | I.1, I.2, I.3 | — | параллельно, без блокировок |

## Жёсткие зависимости

- 3.2 нельзя начинать раньше 3.1: без извлечённого текста нечего писать в vault.
- 4.1 — гейт всей M4: если ни одна доступная модель не принимает `image`,
  слои 6–7 упираются в выбор платной мультимодальной модели, и это решение
  владельца (цена), а не разработки.
- 5.x переиспользует контур M4 — отдельного приёмника изображений не делать.
- I.2 не блокирует ничего, но до неё нельзя обещать пользователю конкретные
  суточные лимиты бесплатных моделей.

## Реестр блокеров

| Блокер | Что блокирует | Что снимет |
|---|---|---|
| Inline-кнопки 👍👎 не подтверждены в hermes (`ТЗ.md` §13.5) | UI слоя 9 | Проверка в бою или ответ мейнтейнеров hermes |
| `channel_post` в hermes не подтверждён (`ТЗ.md` §13.7) | Приёмник канала-дневника | Тот же источник |
| Бэкап vault-а не решён (`ТЗ.md` §13.1) | Обещания сохранности данных | Решение владельца: том amvera vs git/Obsidian Sync |

## Правила цикла

1. Одна задача за цикл: взять → доделать → закоммитить → запушить.
2. Изменился дизайн — сначала правится `ТЗ.md`/`PRD.md`/`Promts/*`, потом код.
3. Задача закрыта — строка **переезжает** в `DONE.md` с датой.
4. `[BLOCKED]` не снимается «по дороге»: причина либо снята, либо маркер остаётся.
